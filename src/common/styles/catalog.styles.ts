import { styled, Typography } from '@mui/material';

export const HeadingTypography = styled(Typography)(() => ({
  fontWeight: 700,
  marginBlock: '1em',
  fontSize: '1.5em',
  lineHeight: 1.25,
  wordBreak: 'break-all',
}));
