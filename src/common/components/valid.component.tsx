import React from 'react';
import CheckTwoToneIcon from '@mui/icons-material/CheckTwoTone';
import { Color } from '../../theme';

const Valid = () => {
  return <CheckTwoToneIcon sx={{ fontSize: '16px', color: Color.positive }} />;
};

export default Valid;
